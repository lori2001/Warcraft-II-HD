/*#include "Unit.h"

Unit::Unit(sf::Vector2f position)
{
	shape_.setPosition(position);
	shape_.setSize({200, 200});
}

void Unit::draw(sf::RenderTarget& target, sf::RenderStates states) const
{
	target.draw(shape_);
}
*/